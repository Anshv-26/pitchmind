requests>=2.31
pandas>=2.1
numpy>=1.26
pyarrow>=14
pytest

# Stage 1 model training (backend/app/ml/{baselines,training,evaluation}.py)
scikit-learn>=1.5
xgboost>=2.0
catboost>=1.2

# Score models: Poisson / Dixon-Coles (backend/app/ml/score_models.py).
# Already present transitively via scikit-learn; declared explicitly since
# score_models.py imports it directly (scipy.optimize, scipy.stats, scipy.special).
scipy>=1.11

# Explanation layer (backend/app/ml/explanations.py): joblib serializes the
# frozen baseline_strength_trio inference artifact. Already present
# transitively via scikit-learn; declared explicitly since explanations.py
# imports it directly.
joblib>=1.3

# Current/live football data layer (backend/app/services/football_data/).
# pydantic: provider-neutral schemas, chosen over dataclasses because FastAPI
# (already the committed stack) depends on it and it validates untrusted
# provider payloads. httpx: HTTP client for the provider adapters.
pydantic>=2
httpx>=0.27
